# Ref: https://docs.powersync.com/self-hosting

source:
  type: postgres
  uri: !env PS_DATABASE_URL

storage:
  type: postgres
  uri: !env PS_DATABASE_URL

client_auth:
  jwks_urls:
    - !env PS_POWERSYNC_JWKS_URL

sync_rules:
  path: /config/sync-rules.yaml