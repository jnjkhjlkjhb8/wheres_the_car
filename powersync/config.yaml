source:
  type: postgresql
  uri: !env PS_DATABASE_URL

storage:
  type: postgresql
  uri: !env PS_DATABASE_URL

client_auth:
  jwks_uris:
    - http://router:8080/api/.well-known/jwks.json

sync_rules:
  path: /config/sync-rules.yaml
